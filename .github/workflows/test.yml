name: deploy test
run-name: deploy test by ${{ github.actor }}
on: [push]
jobs:
  ssh-test:
    name: deploy
    runs-on: ubuntu-latest
    steps:
    - name: testing ssh
      uses: Ujan-Shakya/react-app@ssh
      with:
        host: 23.23.146.36
        username: ubuntu
        key: ${{ secrets.AWS_KEY_WOLO }}
        port: 22
        script: |
           cd /opt 
           ls
           pwd
